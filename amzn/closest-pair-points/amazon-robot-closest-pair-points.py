def getClosestPair(posX, posY):
    pos = []
    for i in range(len(posX)):
        pos.append((posX, pos))
    pos.sort(key=lambda val: )
    # store the positions in an array 

def isSequenceSurvivable(pieces):
